# 维护约定

这个仓库从"堆内容"进入"维护"阶段后，按下面的规矩来。

## 一 · 分支与合并

- **main 不直接提交。** 每个改动走一条分支，命名 `codex/<主题>`（例如 `codex/lateralus`）、
  修 bug 用 `fix/<主题>`。
- 一条分支**只做一件事**。内容章节和交互修复不要混在同一个 PR 里——
  出了问题时能整条回滚，是分支存在的全部意义。
- 合并前必须过 CI（`.github/workflows/ci.yml` 会跑 `scripts/audit-music.mjs`）。
  本地也请先跑一遍：

  ```bash
  node scripts/audit-music.mjs      # 数据审计（乐理数字、页面、编号）
  python3 scripts/serve.py 5200     # 本地预览（禁缓存）
  ```

## 二 · 内容改动的固定动作

1. **引用真实作品的事实**（速度、调性、拍号、时长、署名、发行日期）必须能指出出处；
   能在审计里表达的，就加进审计 —— 我们已经被"数字散在各处、改一处漏一处"坑过。
2. **外部链接必须 curl 过**，200 才写进页面。中国大陆访问不了的站点（如 Wikipedia）
   要在链接后面标注"（需代理）"。
3. **图片**：单张不超过 400KB（审计会拦）。能用 SVG 或 MuseScore 命令行渲染的，
   就不要用界面截图。
4. **实验台的示意素材**必须说明"这是本站自己写的，不是原曲"，
   并且不能暗示它等于原曲的旋律或拍号。

## 三 · 编号规则

- 第一部分、第二部分用数字（0 起连号），第三部分用字母（A 起连号），
  第四部分接着第三部分的字母往下排。
- 字母是**按阅读顺序排的**，所以往中间插入一节，后面的字母要顺延。
  改动时同步三处：`src/js/music/curriculum.js` 的 `no`、
  页面里的 `<span class="crumb">` 和 `<p class="kicker">`。
  审计会检查字母连续性，漏改会当场报错。

## 四 · 交互的既有教训

这些是用户实际指出过的问题，新写实验台时请直接避开：

- **播放前先 `stopAll()`**：不允许两次播放叠在一起（审计会检查 `stopAll` 有没有导入）。
  另外 `main.js` 里有一道**兜底**：点实验台里的任何按钮，会先停掉上一段播放
  （捕获阶段拦截，所以一个点击里排的一串音不受影响）。两类控件要加 `data-keep-audio` 豁免：
  1. **故意叠着放的实验台**；
  2. **播放中只改设定、不该打断播放的控件** —— 声部开关、拍点格子、拍号 / 分组选择、
     摆感 / riff 这类开关（13 节的声部开关、织体分层台、节拍网格就是这么处理）。
     这类按钮点下去不出声，兜底却会把正在响的那一遍抹掉，听感是"点一下就丢拍"。
- **播放必须有指示**：按钮变亮、游标走动、或者读数里显示"正在播放第几段"。
- **拖动条不要在拖动中吸附/重排**：吸附留到松手（`change`），
  数值文字的长度变化不能挤动滑块。
- **听辨题一道题一个作答区**：题干问两件事就写两个 `questions` 条目。
  题面里写的选项数量必须和实际一致。
- **示意素材要留空**：演示"交错/织体"这类概念时，任何一层单独听都必须是不完整的。
