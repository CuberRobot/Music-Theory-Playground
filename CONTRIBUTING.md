# 贡献与维护

Music Theory Playground 是一个**零构建、零依赖**的纯静态教学站：
没有打包器、没有框架、没有 CDN，`lessons/` 里的 HTML 直接就能跑，
所有交互音频都是浏览器里当场合成的。

这份文件是入口。**要加一首新曲子，请直接看 [`docs/新增一首曲子.md`](docs/新增一首曲子.md)**
——那里有逐步的作业流程和检查清单。这里讲的是共同规则。

---

## 一 · 三条底线

这个项目的全部价值压在三条上，任何改动都不能让它们变松：

1. **数据可核对。** 页面里出现的速度、调性、拍号、时长、署名、发行日期，
   要么有出处，要么写进 `scripts/audit-music.mjs`。凭记忆写下来的数字一律不接受。
2. **页面要快。** 单张图片不超过 400KB；能不用二进制的就不用（SVG、CSS、代码画的符号优先）；
   不引入外部依赖。
3. **交互必须有反馈。** 点一下要看得见它在响、在走、还是结束了；
   不允许"点了像没点"的按钮。

## 二 · 问题往哪里交

**统一走 GitHub Issues**，按下面三类模板选一个：

| 你要说的是 | 选哪个 |
| --- | --- |
| 某个按钮／滑块／播放出了毛病 | 🐞 交互问题 |
| 某页的乐理说法、数字、出处有问题 | 📖 内容错误 |
| 想加一首曲子／一个风格章节 | 🎵 新曲目提案 |

问题很小（错别字、链接失效）时，直接开 PR 改掉也可以，不必先开 issue。

**内容错误请尽量写清三件事**：哪一页、哪一句、你认为正确的是什么（有出处最好）。
本站的做法是"改内容的同时，把防线加进审计"——所以你能指出来的错误，
最终都会变成一条自动检查。

## 三 · 分支与 PR

- **`main` 不直接提交。** 每个改动开一条分支：
  - 新内容：`codex/<主题>`（例如 `codex/lateralus`）
  - 修 bug：`fix/<主题>`
  - 文档与规范：`docs/<主题>`
- **一条分支只做一件事。** 新章节和交互修复不要混在一个 PR 里——
  出问题时能整条回滚，是分支存在的全部意义。
- 合并前 CI 必须绿（`.github/workflows/ci.yml` 会跑数据审计）。
- 合并方式：单一主题的分支用 **merge commit**（保留分支边界）；
  一串零碎修补提交先用 `git rebase -i` 整理再合。
- PR 描述用仓库模板，写清「改了什么 / 怎么验的 / 影响范围」。

### 提交信息

形如 `<类型>: <一句话>`：

```
feat:  S 节 Tool Lateralus（小节长度台）
fix:   律制滑块拖动时抽搐（吸附改为松手生效）
docs:  补维护规范与 issue 模板
chore: 加 MuseScore 桥客户端
merge: 合并 codex/lateralus
```

不要用半角双引号包中文标题（会被 shell 吃掉），中文引号用「」。

## 四 · 本地怎么跑

```bash
python3 scripts/serve.py 5200     # 本地预览，禁缓存（改完直接刷新）
node scripts/audit-music.mjs      # 数据审计：乐理数字、页面、编号、实验台注册
node scripts/check-links.mjs      # 外链体检（需要代理的站加 --proxy）
```

审计里的每个数字都应该能追到出处。**它红了就不要开 PR。**

外链体检分四档：`ok`（200/204）、`反爬`（站点拦脚本，用浏览器点一次确认）、
`需要代理`（Wikipedia 这类，加 `--proxy` 再跑）、`x`（真的挂了，必须换）。
**这一档不是走过场**——它已经抓出过四个"当初没核过就写上去"的手册链接。

改完页面还请人工过一遍：控制台没报错；新加的按钮有声音也有指示；
窄屏（手机宽度）看一眼，别把键盘／格子图挤坏。

## 五 · 目录地图

```
index.html                 首页（项目门面，不放交互）
lessons/<id>/index.html    每一节课（真正的正文）
src/js/music/              乐理纯函数：音高、音程、和弦、音阶、节奏
src/js/audio/              合成引擎、排期、速度表
src/js/widgets/            实验台（一个文件一个台，main.js 里注册）
scripts/audit-music.mjs    数据审计（唯一的质量门）
scripts/serve.py           本地静态服务器
assets/musescore/          示例谱（MusicXML）与渲染图
docs/                      方案、章节大纲、UI 规范、新增曲子的流程
```

**唯一真源**：课程结构只在 `src/js/music/curriculum.js` 里写一次，
左侧目录、课程地图、上一课／下一课全部由它渲染。加课、改编号都改这里。

## 六 · 评审要点

1. **音乐对不对**：数字、调性、拍号、术语有没有出处？听起来是不是那么回事？
2. **是不是在教人**：有没有把"现象"变成"可以自己验算的东西"？
3. **交互有没有坑**：播放会不会叠加？拖动会不会跳？点了有没有反馈？
4. **会不会变慢**：新的图片／字体／外部资源有多大？
5. **审计动了吗**：这次引用的新事实，有没有变成一条防线？

## 七 · 版权边界

本站分析的多是**还在版权期内的现代作品**，所以：

- 不放原曲音频、不放原谱截图，也不生成"像原曲一样"的片段；
- 实验台里的素材一律标明「本站自己写的示意，不是原曲」；
- 需要听原曲时，只给**合法来源的链接**（流媒体、官方乐谱购买页）；
- 引用他人的描述（条目、访谈、评论）要写明出处；
- 中国大陆访问不了的站点，链接后面标「（需代理）」。
